extends ../layouts/common
block content
  div(class="mt-4 ml-4")
    h3 Cập nhật tiêu đề sách
    form(action="/books/update", method="POST")
      lable Tiêu đề mới :
      hr
      input(type="hidden", name="id", value=id)
      input(type="text", name="title")
      button.btn.btn-info Cập nhật